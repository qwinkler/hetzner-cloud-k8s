apiVersion: kubeadm.k8s.io/v1beta2
kind: ClusterConfiguration
kubernetesVersion: {{ kubernetes_version }}
clusterName: "{{ kubernetes_cluster_name }}"
networking:
  podSubnet: {{ kubernetes_pod_cidr }}
  serviceSubnet: {{ kubernetes_service_cidr }}
apiServer:
  extraArgs:
    advertise-address:  {{ hetzner_private_network_ip }}
    {# oidc-client-id: {{ kubernetes_oidc_client_id }}
    oidc-groups-claim: {{ kubernetes_oidc_groups_claim }}
    oidc-issuer-url: {{ kubernetes_oidc_issuer_url }}
    oidc-username-claim: {{ kubernetes_oidc_username_claim }} #}